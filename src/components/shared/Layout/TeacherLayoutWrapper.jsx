import RoleLayout from './RoleLayout';

/**
 * Teacher Layout wrapper using the unified RoleLayout
 * This replaces the old TeacherLayout.jsx
 */
const TeacherLayoutWrapper = () => {
    return <RoleLayout role="TEACHER" />;
};

export default TeacherLayoutWrapper;
