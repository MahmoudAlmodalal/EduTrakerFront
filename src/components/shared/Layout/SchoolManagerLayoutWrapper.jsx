import RoleLayout from './RoleLayout';

/**
 * School Manager Layout wrapper using the unified RoleLayout
 */
const SchoolManagerLayoutWrapper = () => {
    return <RoleLayout role="SCHOOL_MANAGER" />;
};

export default SchoolManagerLayoutWrapper;
