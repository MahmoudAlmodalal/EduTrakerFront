import RoleLayout from './RoleLayout';

/**
 * Workstream Manager Layout wrapper using the unified RoleLayout
 */
const WorkstreamLayoutWrapper = () => {
    return <RoleLayout role="WORKSTREAM_MANAGER" />;
};

export default WorkstreamLayoutWrapper;
